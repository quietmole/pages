{
  "devDependencies": {
    "express": "^4.13.3",
    "gulp": "^3.9.0",
    "gulp-html-replace": "^1.5.4"
  },
  "scripts": {
    "build": "gulp build",
    "serve": "node server.js",
    "start": "npm run build && npm run serve"
  }
}
